# Core AI & AWS SDK
boto3>=1.34.0
langchain>=0.1.0
langchain-aws>=0.1.0

# Frontend & Networking
streamlit>=1.30.0
pyngrok>=7.0.0

# Critical Dependency Fixes
# Numpy must be <2.0 to prevent conflicts with current AWS/TensorFlow libraries
numpy<2.0
